import { Layer } from 'sapien/core/layers';
import { Tensor } from 'sapien/core/types';

export class Dense extends Layer{
    feedForward(input:Tensor):Tensor{
        
        return new Tensor();
    }
}
